{
    "name": "peterportal-root",
    "version": "0.1.0",
    "scripts": {
      "postinstall": "npm install --prefix api && npm install --prefix site && npm run build --prefix site",
      "start": "npm start --prefix api"
    }
  }