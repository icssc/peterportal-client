{
  "name": "peterportal-root",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix api && npm install --prefix site"
  },
  "dependencies": {
    "dotenv-flow": "^3.2.0",
    "sst": "^2.5.8"
  },
  "engines": {
    "node": ">=18.x.x"
  }
}
